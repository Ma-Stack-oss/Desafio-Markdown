# 🎯 Desafio GitHub e Markdown

Este é o meu projeto para aprender sobre colaboração e documentação.

* # 🚀 Desafio de Colaboração e Markdown (DIO)

Este repositório foi criado como parte do **Curso de Git/GitHub** na **(DIO)**. O objetivo principal é praticar o fluxo de trabalho de versionamento, colaboração com Pull Requests e a criação de documentação técnica usando a linguagem **Markdown**.

---

## 🛠️ Comandos Git Essenciais (Para o Workflow Diário)

Aqui estão os comandos Git mais utilizados que guiam o nosso fluxo de desenvolvimento e colaboração:

| Comando | Descrição | Uso Típico |
| :--- | :--- | :--- |
| `git clone <url>` | Baixa uma cópia do repositório remoto para sua máquina local. | `git clone https://github.com/usuario/projeto.git` |
| `git checkout -b <branch>` | Cria uma nova *branch* e troca para ela imediatamente. | `git checkout -b feature/nova-funcionalidade` |
| `git add .` | Adiciona todas as alterações feitas na área de *staging* (prontas para o *commit*). | Após modificar arquivos. |
| `git commit -m "msg"` | Salva as alterações na linha do tempo local (histórico do Git). | `git commit -m "feat: Adiciona menu lateral"` |
| `git push -u origin <branch>` | Envia o novo *branch* e seus *commits* para o GitHub. | Para publicar o seu trabalho. |
| `git pull origin <branch>` | Baixa e funde as últimas alterações do repositório remoto. | Antes de começar a trabalhar, para se manter atualizado. |

---

## 🤝 Colaboradores e Acesso

O projeto segue um fluxo de trabalho colaborativo, onde todas as contribuições devem ser feitas via **Pull Request (PR)** para a *branch* principal (`main`), garantindo a revisão do código antes da fusão.

### Proprietário Principal (Admin)

| Nome de Usuário | Função |
| :--- | :--- |
| **Ma-Stack-oss** | Proprietário, responsável pela revisão e *merge* final. |
| **MatheusMtSouza** | Colaborador, autorizado a abrir *branches* e Pull Requests. | **Write** (Escrita) |

### Contribuidores Ativos

O repositório está configurado para permitir a contribuição da seguinte conta (simulando um colega de equipe):

> **Nota de Acesso:** A conta `Ma-Stack-oss` possui permissão de **Write** no GitHub, conforme configurado na seção **Settings > Collaborators**, o que permite que ela faça o `git push` de suas *branches* de desenvolvimento.

---

### 📝 Próximos Passos (Para o Leitor)

1.  Crie sua *branch* de contribuição.
2.  Faça suas alterações.
3.  Abra um **Pull Request (PR)** de sua *branch* para `main`.
4.  Aguarde a revisão do Proprietário (`Ma-Stack-oss`).
